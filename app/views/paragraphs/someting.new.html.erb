<div class="container">
  <h2>Give any content</h2>

  <%= form_with model: [@chapter.subject, @chapter, @paragraph], local: true do |f| %>
    <div class="mb-3">
      <%= f.label :title, "Paragraph Title", class: "form-label" %>
      <%= f.text_field :title, class: "form-control", required: true %>
    </div>

    <div class="mb-3">
      <%= f.label :content, "Content", class: "form-label" %>
      <%= f.text_area :content, class: "form-control", rows: 5, required: true %>
    </div>

    <%= f.submit "Generate Questions and Answers", class: "btn btn-success" %>
    <%= link_to "Back", subject_chapters_path(@chapter.subject, @chapter), class: "btn btn-secondary" %>
  <% end %>
</div>
